package com.gotham.cricket.enums;

public enum OfficialResultType {
    AUTO,         // Use automatic calculation from innings scores
    TIE,
    DRAW,
    NO_RESULT,
    ABANDONED,
    DLS_METHOD,
    SUPER_OVER,
    WALKOVER,
    FORFEIT,
    SPLIT_POINTS,
    CUSTOM
}
